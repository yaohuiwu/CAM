package org.cam.core.action;

/**
 * A action register center for different data action.
 *
 * <p>
 *     Different framework or even you business code will create different classes , interfaces . How can we recognize
 *  methods which we prefer to call it actions of  all these classes ?
 *     It's true that there some common actions , for example , Create , Delete , Modify ( Update if you prefer to call
 *  it in a DBM view ),View .
 *     Hibernate , MyBatis , and other ORM framework already created their own object system which we thanks very much,
 *
 * </p>
 */
public class ActionDialectRegister {
}
